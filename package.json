{
  "name": "orbit-blog",
  "version": "1.0.0",
  "description": "Orbit — a tech blog CMS built with Astro",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port ${PORT:-4321}",
    "start": "astro dev --host 0.0.0.0 --port ${PORT:-4321}",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port ${PORT:-4321}"
  },
  "dependencies": {
    "@fontsource/libre-baskerville": "^5.1.1",
    "@fontsource/source-sans-3": "^5.1.1",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
